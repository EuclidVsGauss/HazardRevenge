#ifndef HAZARD_TMPOBJECTTILE_HPP
#define HAZARD_TMPOBJECTTILE_HPP
#include "HazardTypedef.hpp"

struct TileColorMaps {
  int16_t colorMap[256];
};
typedef struct TileColorMaps TileColorMaps, *PTileColorMaps;

#endif
